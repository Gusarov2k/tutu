= form_for(@train) do |f|
  = render 'common/errors', resource: @train
  .field
    = f.label :number
    %br/
    = f.text_field :number
  .actions
    = f.submit "#{ @train.new_record? ? 'Создать' : 'Изменить'}"
